<table>
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
    <% @found.each do |coach| %>
      <tr>
        <td>
          <span class="lookup_link" lookup_id="<%=coach.id%>">
            <%= coach.name %>
          </span>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
