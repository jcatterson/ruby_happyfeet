<%= form_for(@student_attendance) do |f| %>
  <% if @student_attendance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_attendance.errors.count, "error") %> prohibited this student_attendance from being saved:</h2>

      <ul>
      <% @student_attendance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attendance_id %><br>
    <%= f.number_field :attendance_id %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :did_attend %><br>
    <%= f.check_box :did_attend %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
